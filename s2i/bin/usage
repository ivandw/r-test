#!/bin/bash -e
cat <<EOF
This is the s2i-R-Modelos S2I image:
To use it, install S2I: https://github.com/openshift/source-to-image

Sample invocation:

s2i build <source code path/URL> s2i-R-Modelos <application image>

You can then run the resulting image via:
docker run <application image>

Imagen creada para la construccion de modelos en R 
EOF
